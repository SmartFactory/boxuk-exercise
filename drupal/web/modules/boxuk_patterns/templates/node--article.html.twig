{#
/**
 * @file
 * Theme override to display an article node.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
 * - label: The title of the node.
 * - content: All node items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}.
 * - url: Direct URL of the current node.
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: Same as attributes, except applied to the main title tag.
 * - content_attributes: Same as attributes, except applied to the main content tag.
 * - style_data: Custom data from the bundle class getStyleData() method.
 *
 * @see template_preprocess_node()
 */
#}
{% set style_data = node.getStyleData() %}

{# Example: Using data from HeaderPipe #}
{% if style_data.header %}
  <section class="article-header">
    <div class="header-content">{{ style_data.header }}</div>
  </section>
{% endif %}

{# Example: Using data from TitlePipe #}
{% if style_data.title %}
  <div class="title-data">
    <p>Title: {{ style_data.title.text }}</p>
    <p>Formatted: {{ style_data.title.formatted }}</p>
  </div>
{% endif %}

{# Example: Using data from AuthorPipe #}
{% if style_data.author %}
  <div class="author-info">
    <p>By {{ style_data.author.name }} (ID: {{ style_data.author.id }})</p>
  </div>
{% endif %}

{# Example: Using data from DatePipe #}
{% if style_data.dates %}
  <div class="date-info">
    <p>Created: {{ style_data.dates.created.formatted }}</p>
    <p>Last updated: {{ style_data.dates.changed.formatted }}</p>
  </div>
{% endif %}
<article{{ attributes.addClass('node', 'node--type-' ~ node.bundle|clean_class) }}>

  {{ title_prefix }}
  {% if label and not page %}
    <h2{{ title_attributes }}>
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
    </h2>
  {% endif %}
  {{ title_suffix }}

  {% if display_submitted %}
    <footer class="node__meta">
      {{ author_picture }}
      <div{{ author_attributes.addClass('node__submitted') }}>
        {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}
        {{ metadata }}
      </div>
    </footer>
  {% endif %}

  <div{{ content_attributes.addClass('node__content') }}>
    {# Example of using custom style_data from bundle class #}
    {% if style_data.header %}
      <div class="custom-header">{{ style_data.header }}</div>
    {% endif %}

    {{ content }}
  </div>

</article>
