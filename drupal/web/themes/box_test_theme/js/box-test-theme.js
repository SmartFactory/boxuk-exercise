/**
 * @file
 * Box Test Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.boxTestTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
